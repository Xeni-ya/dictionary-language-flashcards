import styles from './missing.module.css';

const Missing = () => {
  return (
    <main className={styles.main}>
      <img src="src\assets\img\error-page.svg" alt="error" className={styles.error__img} />
    </main>
  );
}

export default Missing;